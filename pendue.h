#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char readC();
int win(int* letterfound,int wordsize);
int searchletter(char letter, char word[], int letterfound[]);
int pickword(char *word);
int randompick(int numbermax);


